name: Build Watchlist Image

on:
  push:
    branches: [main]
    paths:
      - "packages/datafeed/**"
      - "packages/fx/**"
      - "packages/benchmark/**"
      - "packages/future/**"
      - "packages/watchlist/**"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/watchlist-image.yml"
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push equicast-watchlist (private)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: packages/watchlist/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/equicast-watchlist:latest
            ghcr.io/${{ github.repository_owner }}/equicast-watchlist:${{ github.sha }}
