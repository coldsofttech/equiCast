[project]
name = "equicast-future"
version = "0.1.0"
description = "Futures contract data extraction, built on equicast-datafeed."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "equicast-datafeed",
    "equicast-metrics",
    "pandas>=2.2",
    "pyarrow>=17.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.11",
    "types-PyYAML>=6.0",
]

[project.scripts]
equicast-future = "equicast_future.cli:main"
equicast-future-plan = "equicast_future.plan:main"

[tool.uv.sources]
equicast-datafeed = { workspace = true }
equicast-metrics = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/equicast_future"]

[tool.pytest.ini_options]
testpaths = ["tests"]
