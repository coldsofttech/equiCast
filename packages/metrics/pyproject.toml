[project]
name = "equicast-metrics"
version = "0.1.0"
description = "Generic risk/performance metrics (volatility, Sharpe ratio, max drawdown, CAGR) for any yfinance symbol."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "equicast-datafeed",
    "pandas>=2.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.11",
]

[tool.uv.sources]
equicast-datafeed = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/equicast_metrics"]

[tool.pytest.ini_options]
testpaths = ["tests"]
