[project]
name = "equicast-etf"
version = "0.1.0"
description = "ETF ticker market data extraction, built on equicast-datafeed."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "equicast-datafeed",
    "equicast-dividends",
    "pandas>=2.2",
    "pyarrow>=17.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.11",
    "types-PyYAML>=6.0",
]

[project.scripts]
equicast-etf = "equicast_etf.cli:main"
equicast-etf-plan = "equicast_etf.plan:main"

[tool.uv.sources]
equicast-datafeed = { workspace = true }
equicast-dividends = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/equicast_etf"]

[tool.pytest.ini_options]
testpaths = ["tests"]
